// Author: Chih-Hsuan (Cindy) Lin
// RandomPage.tsx -- Renders the DrinkGenerator component, which fetches and displays a random cocktail.
import DrinkGenerator from "./SearchRandom";

export default function RandomPage() {
    return (
        <div>
            <DrinkGenerator />
        </div>
    );
}